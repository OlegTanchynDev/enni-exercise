# Copy to terraform.tfvars to override defaults locally.
# (terraform.tfvars is gitignored — never commit real values.)
region      = "eu-west-2"
environment = "staging"
domain_fqdn = "staging.enni.space"
